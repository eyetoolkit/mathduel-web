# mathduel.games — robots.txt
# 注意：此前该路径被 SPA fallback 接管，返回的是首页 HTML，
# 爬虫完全无法解析。必须先在 public/ 放入本文件 + 404.html 才会生效。

User-agent: *
Allow: /

# API 由 Cloudflare Worker 提供，不是可索引页面
Disallow: /api/

# 应用内跳转/状态参数，避免重复内容
Disallow: /*?mode=
Disallow: /*?lang=
Disallow: /*?count=
Disallow: /*?time=

Sitemap: https://mathduel.games/sitemap.xml
